[package]
name = "svg"
version = "0.1.0"
edition = "2024"
description = "Consumer module for the mod-svg crate, generated by https://github.com/bearcove/dylo"

[lib]
crate-type = ["rlib"]

[dependencies]
dylo-runtime = "2.5.0"
conflux = { path = "../conflux" }
noteyre = { version = "0.1.0", path = "../noteyre" }
futures-core = "0.3.31"
bytes = "1.10.1"

[features]
default = []
